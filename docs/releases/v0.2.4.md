# Flockdeck v0.2.4

Fixed Flockdeck failing to recognise a git worktree when it was reached through a symlink - something that happens routinely on macOS, where parts of the file system are themselves symlinks. Worktrees in that situation now resolve correctly.
