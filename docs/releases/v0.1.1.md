# Flockdeck v0.1.1

- Fixed a race condition that could miscount how many panes were starting at once when several launched together.
- A project opened at the filesystem root (`/`) is now named `/` instead of showing the wrong name.
- The install script served from the Flockdeck website no longer requires Go to be installed on your machine.
