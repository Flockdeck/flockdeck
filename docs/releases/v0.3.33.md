# Flockdeck v0.3.33

## The self-hosted image's supply-chain checks run again
Last release's fix got the self-hosted Docker image building again, but
the steps after it — attaching an SBOM to the release, scanning for
vulnerabilities, signing the image — still couldn't run: the workflow
only had read access to the release, and attaching a file to one needs
write. Fixed.
