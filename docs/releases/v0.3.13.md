# Flockdeck v0.3.13

Panes running Flockdeck's own chat client (the direct model API option) now get the same phone chat view and inbox preview as Claude Code panes, rather than those being limited to Claude Code.

Also fixed a bug where a chat-client pane's conversation couldn't be read by the phone's chat view unless its session id was a plain name.
