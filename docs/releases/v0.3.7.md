# Flockdeck v0.3.7

When one agent starts a helper process of its own - as happens during fan-out and worktree set-up - that helper now stays quiet, without showing its own status or sending notifications, until the agent that started it is gone. This cuts down on noise from short-lived helpers in your panes and on your phone.
