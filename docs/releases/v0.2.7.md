# Flockdeck v0.2.7

Fixed a timing bug in how Flockdeck's hooks judge whether the desktop app is reachable while it's closed: that case now uses the real spawn deadline instead of a placeholder one. This makes hook-driven status and notifications more reliable around the times the app itself isn't running.
